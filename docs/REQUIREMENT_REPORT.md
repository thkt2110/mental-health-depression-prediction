Các Quy tắc Bắt buộc (Mandatory Rules):

Văn phong: Học thuật, chuyên nghiệp, súc tích và khách quan (không dùng đại từ nhân xưng ngôi thứ nhất số ít "Tôi/I"). Sử dụng cấu trúc câu rõ ràng, tránh từ ngữ sáo rỗng.

Toán học & Công thức: MỌI công thức toán học (Standardization, Target Encoding, Logistic Regression, XGBoost Loss, v.v.) BẮT BUỘC phải được định dạng chuẩn xác bằng môi trường toán học của LaTeX (ví dụ: \begin{equation} ... \end{equation}). Giải thích rõ từng ký hiệu (notation) bên dưới mỗi công thức.

Hình ảnh & Bảng biểu:

Luôn sử dụng môi trường figure và table.

Mọi hình ảnh/bảng biểu phải có tiêu đề (caption) chi tiết và nhãn (label) để trích dẫn chéo (cross-reference) trong văn bản (ví dụ: "Như được thể hiện trong Hình \ref{fig:roc_curve}...").

Định dạng bảng chuyên nghiệp bằng gói booktabs (\toprule, \midrule, \bottomrule).

Trọng tâm Giải thích: Không chỉ nêu kết quả ("Mô hình đạt 90%"), mà phải giải thích SÂU (Deep Analysis). Đặc biệt chú trọng vào việc giải thích biểu đồ SHAP (Feature Importance) và liên hệ nó với logic thực tế về sức khỏe tâm thần.

Cấu trúc Báo cáo: Tuân thủ nghiêm ngặt các Sections đã được định nghĩa trong REPORT_PLAN.md (từ Project Planning đến References).

Ngôn ngữ: Tiếng Việt